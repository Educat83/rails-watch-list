<h1>My lists genres</h1>

<div class="container">
  <% @lists.each do |list| %>
    <div>
      <ul>
        <li><%= link_to list.name, list_path(list) %></li>
      </ul>
    </div>
  <% end %>
</div>
